---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

const allPrompts = await getCollection('prompts');
const commsPrompts = allPrompts.filter(p => p.data.category === 'communications');

const base = import.meta.env.BASE_URL;

const grouped = commsPrompts.reduce((acc, prompt) => {
  const sub = prompt.data.subcategory || 'Other';
  if (!acc[sub]) acc[sub] = [];
  acc[sub].push(prompt);
  return acc;
}, {} as Record<string, typeof commsPrompts>);
---

<BaseLayout title="Communications & Marketing" description="AI prompts for social media, email marketing, content creation, and storytelling">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="mb-12">
      <div class="text-6xl mb-4">📢</div>
      <h1 class="text-5xl font-bold mb-4">Communications & Marketing</h1>
      <p class="text-xl text-gray-600 max-w-3xl">
        AI prompts for creating social media content, email campaigns, blog posts, press releases, and impact stories that engage your audience and tell your mission effectively.
      </p>
      <p class="mt-4 text-lg">
        <span class="font-semibold text-blue-600">{commsPrompts.length} prompts</span> in this category
      </p>
    </div>

    <div class="bg-purple-50 border-l-4 border-purple-500 p-6 mb-12 rounded-r-lg">
      <h2 class="text-2xl font-bold mb-4">Why Communications Prompts Matter</h2>
      <div class="prose prose-lg max-w-none">
        <p>
          Communications Managers are stretched thin, responsible for 4-5 social platforms, weekly newsletters, blog posts, and press releases—often solo or with minimal support. According to <a href="https://www.nonprofitmarketingguide.com/11-surprising-things-from-the-2024-nonprofit-communications-trends-report/" target="_blank" rel="noopener" class="text-purple-700 underline">2024 Nonprofit Communications Trends research</a>, just over half of communications teams grew or expected to grow, but resource constraints remain tight.
        </p>
        <p>
          The challenge: <a href="https://www.nptechforgood.com/101-best-practices/social-media-statistics-for-nonprofits/" target="_blank" rel="noopener" class="text-purple-700 underline">96% of nonprofits use Facebook</a>, yet creating engaging content consistently is time-consuming. <a href="https://empower.agency/insights/social-media/social-media-stats-for-charities-and-nonprofits/" target="_blank" rel="noopener" class="text-purple-700 underline">80% of social marketers now use AI</a> for editing and refining content—up from 67% in 2023.
        </p>
        <p>
          <strong>Where AI helps:</strong> Draft social posts, create newsletter content, write blog posts from program data, and generate multiple platform-specific versions of the same message—transforming hours of writing into minutes of editing.
        </p>
      </div>
    </div>

    {Object.entries(grouped).map(([subcategory, prompts]) => (
      <div class="mb-12">
        <h2 class="text-2xl font-bold mb-6 pb-2 border-b-2 border-gray-200">{subcategory}</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {prompts.map((prompt) => (
            <a
              href={`${base}prompts/${prompt.slug}`}
              class="bg-white p-6 rounded-lg shadow-sm hover:shadow-md transition border border-gray-200 hover:border-purple-500"
            >
              <div class="flex items-start justify-between mb-3">
                <span class="text-xs font-semibold px-2 py-1 bg-purple-100 text-purple-700 rounded">
                  {subcategory}
                </span>
                <span class="text-xs text-gray-500">{prompt.data.difficulty}</span>
              </div>
              <h3 class="text-lg font-semibold mb-2">{prompt.data.title}</h3>
              <p class="text-gray-600 text-sm mb-4">{prompt.data.description}</p>
              <div class="flex flex-wrap gap-1">
                {prompt.data.role.slice(0, 2).map((role) => (
                  <span class="text-xs px-2 py-1 bg-blue-50 text-blue-700 rounded">{role}</span>
                ))}
              </div>
            </a>
          ))}
        </div>
      </div>
    ))}

    <div class="bg-gray-50 rounded-lg p-8 mt-12">
      <h2 class="text-2xl font-bold mb-4">Communications Best Practices</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <h3 class="font-semibold text-lg mb-2">Social Media</h3>
          <ul class="text-gray-700 space-y-2">
            <li>• Post 2-3 times/week on Facebook for orgs under 10K followers</li>
            <li>• <a href="https://empower.agency/insights/social-media/social-media-stats-for-charities-and-nonprofits/" target="_blank" rel="noopener" class="text-purple-600 underline">96% of nonprofits use Facebook</a></li>
            <li>• Mix content types: 48% should not promote your org directly</li>
          </ul>
        </div>
        <div>
          <h3 class="font-semibold text-lg mb-2">Content Strategy</h3>
          <ul class="text-gray-700 space-y-2">
            <li>• AI adoption for content: 80% now use AI for editing/refining</li>
            <li>• Share stories, not just news (engagement increases)</li>
            <li>• Mobile-first: Most social engagement happens on phones</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>
